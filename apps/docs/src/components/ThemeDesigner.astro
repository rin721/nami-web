---
import { t, type DocsLocale } from '../lib/site';

interface Props {
  locale: DocsLocale;
}

const { locale } = Astro.props;
---

<nami-docs-theme-designer>
  <section class="section" data-theme-designer>
    <div class="section-heading">
      <p class="eyebrow">{t(locale, 'nav.designer')}</p>
      <h2>{t(locale, 'designer.title')}</h2>
      <p>{t(locale, 'designer.copy')}</p>
    </div>
    <div class="theme-designer-grid">
      <nami-card>
        <h3 slot="header">{t(locale, 'designer.seed')}</h3>
        <div class="control-stack">
          <nami-tab-bar data-theme-mode value="light">
            <button value="light">{t(locale, 'controls.light')}</button>
            <button value="dark">{t(locale, 'controls.dark')}</button>
          </nami-tab-bar>
          <div class="swatches" aria-label="Accent">
            <button data-accent="#3b82f6" style="--swatch: #3b82f6">{t(locale, 'controls.blue')}</button>
            <button data-accent="#14b8a6" style="--swatch: #14b8a6">{t(locale, 'controls.teal')}</button>
            <button data-accent="#8b5cf6" style="--swatch: #8b5cf6">{t(locale, 'controls.violet')}</button>
          </div>
          <nami-tab-bar data-radius-mode value="round">
            <button value="sharp">{t(locale, 'controls.radiusSharp')}</button>
            <button value="soft">{t(locale, 'controls.radiusSoft')}</button>
            <button value="round">{t(locale, 'controls.radiusRound')}</button>
          </nami-tab-bar>
          <nami-chip data-density-toggle checkbox value="compact">{t(locale, 'controls.compact')}</nami-chip>
          <nami-switch data-motion-toggle>{t(locale, 'controls.reduced')}</nami-switch>
          <nami-chip data-style-toggle checkbox value="illustration">{t(locale, 'controls.illustration')}</nami-chip>
          <nami-chip data-contrast-toggle checkbox value="high">{t(locale, 'controls.highContrast')}</nami-chip>
        </div>
      </nami-card>
      <nami-card data-designer-preview>
        <h3 slot="header">{t(locale, 'designer.preview')}</h3>
        <div class="theme-live-surface">
          <nami-button>Primary action</nami-button>
          <nami-button variant="soft">Soft action</nami-button>
          <nami-badge variant="primary">Token</nami-badge>
          <nami-chip checkbox selected value="selected">Selected chip</nami-chip>
          <nami-input label="Search" placeholder="--nami-color-primary"></nami-input>
          <nami-switch checked>Enabled</nami-switch>
          <nami-radio-card selected label="Semantic anatomy" description="Parts, states, and tokens stay explainable."></nami-radio-card>
        </div>
      </nami-card>
    </div>
    <div class="theme-designer-output">
      <nami-card>
        <h3 slot="header">{t(locale, 'designer.tree')}</h3>
        <div data-derived-token-tree class="derived-token-tree"></div>
      </nami-card>
      <nami-card>
        <h3 slot="header">{t(locale, 'designer.css')}</h3>
        <nami-docs-code-block data-generated-css data-language="css"></nami-docs-code-block>
      </nami-card>
      <nami-card>
        <h3 slot="header">{t(locale, 'designer.affected')}</h3>
        <div data-affected-components class="affected-components"></div>
      </nami-card>
    </div>
  </section>
</nami-docs-theme-designer>
