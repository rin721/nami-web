{
  "name": "nami-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run localize:build && npm run build:packages && npm run build:apps",
    "build:packages": "npm run build --workspace @nami/tokens && npm run build --workspace @nami/themes && npm run build --workspace @nami/ui",
    "build:apps": "npm run build --workspace @nami/docs",
    "test": "npm run test --workspace @nami/tokens && npm run test --workspace @nami/ui",
    "test:browser": "playwright test",
    "dev": "npm run dev --workspace @nami/docs",
    "manifest": "npm run manifest --workspace @nami/ui",
    "localize:extract": "lit-localize extract",
    "localize:build": "lit-localize build",
    "verify": "npm test && npm run build && npm run manifest && npm run test:browser"
  },
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.11.0",
    "@lit/localize-tools": "^0.8.2",
    "@playwright/test": "^1.60.0",
    "jsdom": "^27.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
